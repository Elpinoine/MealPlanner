@page
@model MealPlanner.Pages.EntryMealRelation.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PlanEntryMealRelation[0].PlanEntry)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PlanEntryMealRelation[0].Meal)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.PlanEntryMealRelation) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PlanEntry.ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Meal.ID)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
