@page
@model MealPlanner.Pages.Entries.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PlanEntry[0].Plan)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PlanEntry[0].Meal)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PlanEntry[0].Date)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.PlanEntry) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Plan.ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Meal.ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
